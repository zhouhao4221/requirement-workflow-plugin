---
description: 查看需求详情 - 阅读需求完整内容
argument-hint: "[REQ-XXX]"
allowed-tools: Read, Glob, Grep
model: claude-haiku-4-5-20251001
---

# 查看需求详情

阅读需求文档的完整业务内容，纯只读，不改变任何状态。

与 `/req:status`（查看进度）互补：`status` 回答"进展如何"，`show` 回答"需求是什么"。

## 命令格式

```
/req:show [REQ-XXX|QUICK-XXX] [--summary]
```

**说明**：
- 编号可选，省略时自动选择最近活跃的需求
- `--summary`：仅展示摘要（元信息 + 需求描述 + 功能清单）

---

## 执行流程

### 0. 自动识别需求

如果未提供编号：

```python
# 根据角色决定搜索路径（步骤 1 中已解析 ROLE 和路径）
# readonly：直接搜索缓存 active/
# primary：搜索本地 active/，本地为空时搜索缓存 active/
# 未绑定：仅搜索本地 active/
candidates = find_requirements(dir=ACTIVE, sort_by="mtime")

if len(candidates) == 0 and ROLE == "primary" and CACHE_ACTIVE:
    candidates = find_requirements(dir=CACHE_ACTIVE, sort_by="mtime")

if len(candidates) == 0:
    print("没有活跃的需求")
    if ROLE != "readonly":
        print("创建新需求：/req:new")
    exit()
elif len(candidates) == 1:
    REQ_ID = candidates[0]
    print(f"自动选择：{REQ_ID}")
else:
    print("发现多个活跃需求，请选择：")
    for i, req in enumerate(candidates):
        print(f"  {i+1}. {req}")
    choice = input()  # 用户输入编号
    REQ_ID = candidates[int(choice) - 1]
```

### 1. 解析存储路径（按角色）

```bash
# 读取项目配置
PROJECT=$(cat .claude/settings.local.json 2>/dev/null | jq -r '.requirementProject // empty')
ROLE=$(cat .claude/settings.local.json 2>/dev/null | jq -r '.requirementRole // empty')

if [ "$ROLE" = "readonly" ]; then
    # 只读仓库：直接使用缓存路径
    ROOT=~/.claude-requirements/projects/$PROJECT
    ACTIVE=$ROOT/active
    COMPLETED=$ROOT/completed
    SOURCE="cache"
elif [ "$ROLE" = "primary" ]; then
    # 主仓库：优先本地，缓存为备用
    LOCAL_ROOT=docs/requirements
    ACTIVE=$LOCAL_ROOT/active
    COMPLETED=$LOCAL_ROOT/completed
    CACHE_ROOT=~/.claude-requirements/projects/$PROJECT
    CACHE_ACTIVE=$CACHE_ROOT/active
    CACHE_COMPLETED=$CACHE_ROOT/completed
    SOURCE="local"
else
    # 未绑定项目：仅使用本地
    LOCAL_ROOT=docs/requirements
    ACTIVE=$LOCAL_ROOT/active
    COMPLETED=$LOCAL_ROOT/completed
    SOURCE="local"
fi
```

### 2. 查找需求文档（按角色）

**`primary` 角色**搜索位置（按优先级）：
1. `$ACTIVE/REQ-XXX-*.md`（本地）
2. `$COMPLETED/REQ-XXX-*.md`（本地）
3. `$CACHE_ACTIVE/REQ-XXX-*.md`（本地不存在时）
4. `$CACHE_COMPLETED/REQ-XXX-*.md`（本地不存在时）

**`readonly` 角色**搜索位置：
1. `$ACTIVE/REQ-XXX-*.md`（缓存）
2. `$COMPLETED/REQ-XXX-*.md`（缓存）

如果未找到：
```
❌ 未找到需求：REQ-XXX

💡 可用操作：
- 查看所有需求：/req
- 创建新需求：/req:new
```

### 3. 读取需求文档

使用 Read 工具读取找到的需求文档完整内容。

### 4. 输出需求详情

根据需求类型（REQ / QUICK）和模式（完整 / 摘要）展示内容。

#### REQ-XXX 完整模式

```
═══════════════════════════════════════════════
📖 需求详情：REQ-001 部门渠道关联
═══════════════════════════════════════════════

📌 元信息
├── 编号：REQ-001
├── 状态：🔨 开发中
├── 类型：后端
├── 模块：系统管理
├── 优先级：P1
├── 负责人：张三
├── 数据来源：本地 (primary)
└── 项目：my-saas-product

───────────────────────────────────────────────

## 一、需求描述

### 1.1 背景
[文档原文...]

### 1.2 目标
[文档原文...]

### 1.3 客户场景
[文档原文...]

### 1.4 价值
[文档原文...]

───────────────────────────────────────────────

## 二、功能清单

- [ ] **功能点1**：描述...
- [x] **功能点2**：描述...
...

───────────────────────────────────────────────

## 三、业务规则

[文档原文...]

───────────────────────────────────────────────

## 四、使用场景

[文档原文...]

───────────────────────────────────────────────

## 五、接口需求

[文档原文...]

───────────────────────────────────────────────

## 六、测试要点

[文档原文...]

───────────────────────────────────────────────

## 十、实现方案

> 仅在已填写时展示，未填写时跳过本章节

[文档原文...]

───────────────────────────────────────────────

## 九、关联信息

[文档原文...]

═══════════════════════════════════════════════

💡 可用操作：
# primary 角色显示完整操作
- 查看进度：/req:status REQ-001
- 继续开发：/req:dev REQ-001
- 编辑需求：/req:edit REQ-001
- 进入测试：/req:test REQ-001

# readonly 角色仅显示只读操作
# - 查看进度：/req:status REQ-001
# - 查看需求列表：/req
```

**展示规则**：
- 一~六章（需求定义区）：**完整展示原文**，这是本命令的核心价值
- 七、八章（评审记录、变更记录）：**跳过**，这些属于流程记录，用 `/req:status` 查看
- 九章（关联信息）：展示，便于了解上下文
- 十章（实现方案）：仅在已填写时展示（判断依据：内容不是模板占位文本 `_开发阶段填充_`）

#### REQ-XXX 摘要模式（--summary）

```
═══════════════════════════════════════════════
📖 需求摘要：REQ-001 部门渠道关联
═══════════════════════════════════════════════

📌 元信息
├── 编号：REQ-001
├── 状态：🔨 开发中
├── 类型：后端
├── 模块：系统管理
├── 优先级：P1
└── 负责人：张三

───────────────────────────────────────────────

## 一、需求描述

[文档原文...]

───────────────────────────────────────────────

## 二、功能清单

[文档原文...]

═══════════════════════════════════════════════

💡 查看完整内容：/req:show REQ-001
```

#### QUICK-XXX 完整模式

```
═══════════════════════════════════════════════
📖 快速需求详情：QUICK-001 修复登录验证码失效
═══════════════════════════════════════════════

📌 元信息
├── 编号：QUICK-001
├── 改动类型：bug修复
├── 端类型：后端
├── 状态：🔨 开发中
├── 模块：用户
├── 优先级：P1
├── 负责人：张三
├── 数据来源：本地 (primary)
└── 项目：my-saas-product

───────────────────────────────────────────────

## 问题描述

### 现象
[文档原文...]

### 期望
[文档原文...]

───────────────────────────────────────────────

## 实现方案

### 问题分析
[文档原文...]

### 解决方案
[文档原文...]

### 涉及文件
[文档原文...]

### 改动量
[文档原文...]

───────────────────────────────────────────────

## 验证方式

[文档原文...]

═══════════════════════════════════════════════

💡 可用操作：
- 查看进度：/req:status QUICK-001
- 继续开发：/req:dev QUICK-001
- 编辑需求：/req:edit QUICK-001
```

#### QUICK-XXX 摘要模式（--summary）

```
═══════════════════════════════════════════════
📖 快速需求摘要：QUICK-001 修复登录验证码失效
═══════════════════════════════════════════════

📌 元信息
├── 编号：QUICK-001
├── 改动类型：bug修复
├── 端类型：后端
├── 状态：🔨 开发中
└── 模块：用户

───────────────────────────────────────────────

## 问题描述

[文档原文...]

═══════════════════════════════════════════════

💡 查看完整内容：/req:show QUICK-001
```

---

## 注意事项

1. **纯只读**：本命令不修改任何文件，不触发缓存同步
2. **展示原文**：直接展示需求文档中的原始内容，不做摘要或改写
3. **跳过占位文本**：如果某章节仅包含模板占位文本（如 `_开发阶段填充_`），标注"（未填写）"并跳过内容
4. **支持 QUICK 和 REQ**：自动识别需求类型，按对应模板结构展示

## 用户输入

$ARGUMENTS
